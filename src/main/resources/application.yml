# ===================================================================
# Spring Boot configuration.
# This configuration will be overridden by the Spring profile you use,
# for example application-dev.yml if you use the "dev" profile.
# ===================================================================

---
# Conditionally disable springdoc on missing api-docs profile
spring:
  config:
    activate:
      on-profile: '!api-docs'
springdoc:
  api-docs:
    enabled: false
---
server:
  servlet:
    session:
      cookie:
        http-only: true
---
spring:
  application:
    name: sapi
  cache:
    type: caffeine # optional — auto-detected anyway
    cache-names: labels, videoTagLabels, intensityLabels, audienceLabels, todayVideos, videoLibrary, videosByCategory # pre-create cache(s) at startup
    caffeine:
      spec: maximumSize=1000, expireAfterWrite=1h # global default
  profiles:
    # The commented value for `active` can be replaced with valid Spring profiles to load.
    # Otherwise, it will be filled in by maven when building the JAR file
    # Either way, it can be overridden by `--spring.profiles.active` value passed in the commandline or `-Dspring.profiles.active` set in `JAVA_OPTS`
    active: dev
    group:
      dev:
        - dev
        - api-docs
        # Uncomment to activate TLS for the dev profile
        #- tls
  jmx:
    enabled: false
  mail:
    host: smtpout.secureserver.net
    port: 465
    username: info.@peecko.com
    password: InfoPeecko@2022
    properties:
      mail.smtp.aut: true
      mail.smtp.starttls.enable: true
      mail.smtp.starttls.required: true
  data:
    jpa:
      repositories:
        bootstrap-mode: deferred
  jpa:
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC
      hibernate.timezone.default_storage: NORMALIZE
      hibernate.type.preferred_instant_jdbc_type: TIMESTAMP
      hibernate.id.new_generator_mappings: true
      hibernate.cache.use_second_level_cache: false # disables second-level cache
      hibernate.cache.use_query_cache: false # optional but recommended — also disables query cache
      hibernate.generate_statistics: false
      hibernate.format_sql: true
      # modify batch size as necessary
      hibernate.jdbc.batch_size: 25
      hibernate.order_inserts: true
      hibernate.order_updates: true
      hibernate.query.fail_on_pagination_over_collection_fetch: true
      hibernate.query.in_clause_parameter_padding: true
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
        implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy
  messages:
    basename: i18n/messages
  main:
    allow-bean-definition-overriding: true
  mvc:
    problem-details:
      enabled: true
  task:
    execution:
      thread-name-prefix: sapi-task-
      pool:
        core-size: 2
        max-size: 50
        queue-capacity: 10000
    scheduling:
      thread-name-prefix: sapi-scheduling-
      pool:
        size: 2
  thymeleaf:
    mode: HTML
  output:
    ansi:
      console-available: true

springdoc:
  show-actuator: true

# ===================================================================
# Application specific properties
# ===================================================================
app:
  version: @project.version@
